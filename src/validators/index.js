import validateCourse, { schema as courseSchema } from './course.validator';
import validateTeacher, { schema as teacherSchema } from './teacher.validator';

export { courseSchema, validateCourse, teacherSchema, validateTeacher };
